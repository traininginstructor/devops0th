name: Merge Repository develop to main
on:
  pull_request:
    branches:
      - master

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - name: merge pull request
        run: gh pr merge ${{ https://${{github.actor}}:${{secrets.GITHUB_TOKEN}}@github.com/${{github.repository}}.git }} --merge
